spring:
  application:
    name: Hyrule

  datasource:
    url: jdbc:postgresql://localhost:5432/hylure_db
    username: postgres
    password: admin123

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true